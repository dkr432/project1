#팀원: 당곡고 이마루, 이한규
# 2회차 (2026.05.18)

## 오늘 한 일
1.깃허브에 main.py 파일 만들기
2.LOG.md 파일 만들기
3.코드 짜기

##오늘 한 일
임시로 정한 값이기 때문에 실제에서는 다를 수 있기 때문에 사이트에서 임계값을 수정할 수 있게 만들었다.
임계값을 정해서 가스가 특정 수치에 도달해야 타이머가 작동하게 만들었다.
# 첫번째 시도 - 실패
임시로 정한 값이기 때문에 실제에서는 다를 수 있기 때문에 사이트에서 임계값을 수정할 수 있게 만들었다.
임계값을 정해서 가스가 특정 수치에 도달해야 타이머가 작동하게 만들었다.
슬라이더를 움직여도 처음 임계값으로 돌아오는 버그가 발생해서 실패함.

#두번쨰 시도 - 성공
임계값 변수를 처음에만 지정하고 다음부터는 슬라이더로 입력 받은 값으로 변경하도록 함.
웹사이트에 가스 수치가 표기 안됨.
피코에 아날로그와 디지털 센서를 반대로 꽂아서 다시 위치를 바꾸어 해결함.

#세번쨰 시도 - 성공
웹사이트에 가스 수치 입력 안되는게 가스 센서의 오류인줄 알았지만 웹사이트를 2번쨰 시도에서 수정한 후 가스 수치가 표기가 안되는 오류가 또 발생함.
웹사이트를 여는 방법을 함수로 바꾸고, 원래는 데이터를 한번에 다 보냈지만 피코 용량 때문에 512바이트 씩 나눠서 보내게 하였으며, led 타이밍을 변수로 입력해서 사용했었는데, 변수를 없애고 기본값으로 사용하여 해결함.

## 알게 된 것
Ai가 짜준 코드가 완벽하지는 않다는걸 암.
중간에 디지털 신호와 아날로그 신호를 구분 못했어서 발생한 문제들이 있었는데, 이를 통해 신호 구분의 중요성을 알게 됨.


## 다음 시간 할 일
웹사이트 디자인을 바꾼 후 디스코드에 메세지를 보낼 수 있게해야함.
타이머 작동을 확인하고 수정해야함.

# 2.5회차(2026.5.19)

## 오늘 한 일
1. 웹사이트 테마 변경하기
2. 기본적인 오류들 확인하고 고치기
3. 어떻게 업그레이드 시킬지 아이디어 고민하기

#첫번째 오류
구상한 led 사용법은 타이머의 남은시간에 따라, 또는 문제가 발생했을때 led가 반응해서 점등해야하는데, led가 아예 먹통인 오류가 발생함.
저번 시간에 코드 수정 중에, 타이밍 변수를 없앴었는데, 그게 문제 였음. 타이밍 변수를 정확히 입력하여 해결함.

#두번째 오류
슬라이더로 설정하는 주의, 위험 임계값에 따라 led의 색이 변하고, 웹사이트에서도 보여야하는데, 임계값이 아닐때 주의 표시가 뜨고, 임계값을 내려도 주의 표시가 안사라지는 오류가 발생함.
